{
  "name": "resq-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Multi-Hazard Disaster Intelligence & Response System for Chennai",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue.bold,cyan.bold\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:all": "concurrently -n \"BACKEND,FRONTEND,FASTAPI\" -c \"blue.bold,cyan.bold,magenta.bold\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:fastapi\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend/express-server && npm run dev",
    "dev:fastapi": "cd backend/fastapi-server && python -m uvicorn main:app --host 0.0.0.0 --port 8008 --reload",
    "build": "cd backend/express-server && npm run build && cd ../../frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend/express-server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
